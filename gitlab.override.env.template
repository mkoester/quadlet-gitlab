# Local overrides — values here take precedence over gitlab.env.
# This file is NOT committed to version control (see .gitignore).
# Instance-specific configuration belongs in gitlab.rb (see gitlab.rb.template).
